<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendance_fingerprint">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attendance_FingerPrint">
        <activity android:name=".AddAttandanceSessionActivity"></activity>
        <activity android:name=".ViewStudentByBranchYear" />
        <activity android:name=".ViewStudentActivity" />
        <activity android:name=".ViewFacultyActivity" />
        <activity android:name=".AddFacultyActivity" />
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".Menu" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>